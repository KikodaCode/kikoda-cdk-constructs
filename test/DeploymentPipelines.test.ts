// import { DeploymentPipelines } from '../src';

describe('AllowCodeStarSnsPublishStatement', () => {
  it('should create without error.', () => {
    //const statement = new DeploymentPipelines();
    //expect(statement).not.toBeNull;
  });
});
