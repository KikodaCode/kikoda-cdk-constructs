export * from './document';

/** This construct lib serves as the missing L2 extension
 * lib for the aws-cdk-lib/aws-ssm module, so we'll also export
 * the rest of the constructs from aws-cdk-lib/aws-ssm here.
 */
export * from 'aws-cdk-lib/aws-ssm';
