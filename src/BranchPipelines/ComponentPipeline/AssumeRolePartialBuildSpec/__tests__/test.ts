import { AssumeRolePartialBuildSpecSpec } from '..';

describe('AssumeRolePartialBuildSpecSpec', () => {
  it('should do the thing its ment to do', () => {
    new AssumeRolePartialBuildSpecSpec('arn:aws:iam:::role');
  });
});
